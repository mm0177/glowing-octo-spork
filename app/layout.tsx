import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ask India",
  description:
    "Ask a question, hear from thousands of synthetic Indian personas — visualised on a live map.",
  openGraph: {
    title: "Ask India",
    description:
      "Interactive synthetic focus group powered by NVIDIA Nemotron-Personas-India and Groq.",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <head>
        {/* Leaflet CSS served as static file — avoids Turbopack CSS bundling issues */}
        <link rel="stylesheet" href="/leaflet.css" precedence="default" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
